//Author: Spirus
#pragma GCC optimize("Ofast")
#include<bits/stdc++.h>
using namespace std;
using ll=long long int;

void solution(){
  int counter=0,n,sakura;
  set<int> s;
  cin>>n;
  cin>>counter;
  for(int i=0; i++<counter;s.insert(sakura)){
      cin>>sakura;
  }
  counter=0;
  cin>>counter;
  for(int i=0; i++<counter;s.insert(sakura)){
      cin>>sakura;
  }
  s.size()==n?cout<<"I become the guy.":cout<<"Oh, my keyboard!";
}
   
int main(){
ios_base::sync_with_stdio(0); cin.tie(NULL);
solution();
return 0;
}